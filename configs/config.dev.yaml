http:
  addr: ":8080"

database:
  dsn: "memory"

providers:
  enabled: ["intercity", "synth"]
  intercity:
    reestr_path: "data/reestr/regions.json"

log:
  level: "debug"

planner:
  engine: "raptor"

auth:
  admin_token: "${ADMIN_TOKEN:-dev-secret}" # dev fallback

yandex:
  rasp_key: ""
  geocode_key: ""
