http:
  addr: ":8080"

database:
  dsn: "memory"

providers:
  enabled: ["synth"]
  intercity:
    reestr_path: "testdata/reestr/mini.json"

planner:
  engine: "csa"

auth:
  admin_token: "test-secret"
