# TravelMCP — пример окружения. Скопируйте в .env и заполните секреты.
# Приоритет env > .env > YAML. Префикс TRAVELMCP__ для вложенных ключей (см. docs/10-observability.md).
# Не коммитьте .env с реальными ключами.

# HTTP
HTTP_ADDR=:8080
# TRAVELMCP__HTTP__RATE_LIMIT__RPS=100
# TRAVELMCP__HTTP__RATE_LIMIT__BURST=200
# HTTP_RATE_LIMIT_RPS=100
# HTTP_RATE_LIMIT_BURST=200

# Store (единая точка — store.dsn)
# memory (тесты/dev): memory | sqlite файл: sqlite://./data/travelmcp.db | postgres: postgres://user:pass@localhost:5432/travelmcp?sslmode=disable
DATABASE_DSN=
# TRAVELMCP__STORE__DSN=sqlite://./data/travelmcp.db
# TRAVELMCP__STORE__KIND=memory
# TRAVELMCP__CACHE__KIND=memory
# TRAVELMCP__CACHE__TTL=5m
# TRAVELMCP__QUEUE__KIND=memory

# Providers
PROVIDERS_ENABLED=
# synth demo: PROVIDERS_ENABLED=synth
# TRAVELMCP__PROVIDERS__ENABLED=synth

# Planner
PLANNER_ENGINE=csa
# PLANNER_SEMAPHORE_SIZE=0
# PLANNER_SEMAPHORE_ENABLE=true
# TRAVELMCP__PLANNER__SEMAPHORE_SIZE=8

# Auth
ADMIN_TOKEN=dev-secret
# TRAVELMCP__AUTH__ADMIN_TOKEN=dev-secret

# Geocoder — приоритет по умолчанию nominatim (OSM, бесплатно) -> yandex.
# kind не обязателен (пусто = nominatim first); при недоступности — фолбэк.
# Ключ Яндекса — только YANDEX_GEOCODE_KEY (секция yandex ниже).
GEOCODER_KIND=
GEOCODER_ATTEMPTS=3
GEOCODER_LIMIT=5
GEOCODER_MAX_CALLS=0
GEOCODER_TTL_VERIFIED=2160h
GEOCODER_TTL_DISPUTED=168h
NOMINATIM_URL=https://nominatim.openstreetmap.org
# TRAVELMCP__GEOCODER__KIND=
# TRAVELMCP__GEOCODER__ATTEMPTS=3
# TRAVELMCP__GEOCODER__LIMIT=5
# TRAVELMCP__GEOCODER__MAX_CALLS=0
# TRAVELMCP__GEOCODER__TTL_VERIFIED=2160h
# TRAVELMCP__GEOCODER__TTL_DISPUTED=168h
# TRAVELMCP__NOMINATIM__URL=https://nominatim.openstreetmap.org

# Yandex Rasp (расписания) + Geocode (fallback геокодера)
YANDEX_RASP_KEY=
YANDEX_RASP_URL=https://api.rasp.yandex.net/v3.0
YANDEX_GEOCODE_KEY=
YANDEX_GEOCODE_URL=https://geocode-maps.yandex.ru/1.x

# Log — по умолчанию берётся из YAML (config.dev.yaml: debug/text, config.example.yaml: info/json)
# Раскомментируйте чтобы переопределить YAML (приоритет env > .env > YAML):
# LOG_LEVEL=info
# LOG_FORMAT=json
# LOG_ADD_SOURCE=false
# Per-модуль: TRAVELMCP__LOG__LEVELS__HTTP=debug, TRAVELMCP__LOG__LEVELS__STORE=debug
# или LOG_LEVEL=debug для всего

# Telemetry
LOG_LOKI_URL=http://localhost:3100
LOG_LOKI_ENABLED=true