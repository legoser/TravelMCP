# 06. Структура (актуальная → архив)

> До `14-plan.md` эта документация описывала каркас `memory/sqlite` + `CSA`. С `13/14` каркас пересобирается под `Postgres+PostGIS` + коннекторы + один `gtfs.zip`. Раздел оставлен как снимок, новая карта — в `14-plan.md:1`.

Снимок см. git history до `2026-09-04`. Далее — `14-plan.md:1` `Архитектура` и `14-plan.md:3` `Структура БД`.

